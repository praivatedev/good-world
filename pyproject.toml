[project]
name = "ass-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mike-PG65", email = "petergonye123@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[project.scripts]
ass-project = "ass_project:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.fastapi]
entrypoint = "my:app"